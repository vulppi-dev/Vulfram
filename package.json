{
  "name": "vulfram-core",
  "version": "0.0.1",
  "main": "index.node",
  "types": "index.d.ts",
  "scripts": {
    "build:ffi": "cargo build --release --features ffi --no-default-features",
    "build:napi": "cargo build --release",
    "build:lua": "cargo build --release --features lua --no-default-features",
    "build:python": "cargo build --release --features python --no-default-features",
    "debug:ffi": "cargo build --features ffi --no-default-features",
    "debug:napi": "cargo build",
    "debug:lua": "cargo build --features lua --no-default-features",
    "debug:python": "cargo build --features python --no-default-features"
  },
  "devDependencies": {
    "@types/bun": "^1.3.3"
  }
}
